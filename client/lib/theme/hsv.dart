

import 'package:flutter/material.dart';

const blackHSV = HSVColor.fromAHSV(1.0, 0.0, 0.0, 42.0 / 256.0); // 48 == 30 HEX making text not so black
const whiteHSV = HSVColor.fromAHSV(1.0, 0.0, 0.0, 1.0);